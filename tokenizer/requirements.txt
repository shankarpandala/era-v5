# Assignment 2 (resubmission) — grader-compatible pipeline
tokenizers       # HuggingFace tokenizer (the grader loads tokenizer.json with Tokenizer.from_file)
regex            # \p{...} for the faithful-unit metric
requests         # corpus fetch (Wikipedia REST HTML)
beautifulsoup4   # HTML cleanup before markdown conversion
lxml             # bs4 parser
markdownify      # HTML -> faithful Markdown
